{
    "layer": "top",
    "margin-top": 0,
    "margin-bottom": 0,
    "margin-left": 0,
    "margin-right": 0,
    "spacing": 0,

    "include": [
        "~/.config/waybar/modules.json"
    ],

    "modules-left": [
        "custom/appmenu",
        "wlr/taskbar",
        "niri/window"
    ],

    "modules-center": [
        "niri/workspaces"
    ],

    "modules-right": [
        "tray",
        "backlight",
        "pulseaudio",
        "network",
        "clock",
        "battery",
        "custom/exit"
    ],
// Add this module definition at the bottom of your config
    "network": {
    "format-wifi": "  {essid}",
    "format-ethernet": " {ipaddr}",
    "format-linked": "{ifname} (No IP)",
    "format-disconnected": "⚠ Disconnected",
    "format-alt": "{ifname}: {ipaddr}/{cidr}",
    "tooltip-format": "{ifname} via {gwaddr}"
    }
}
